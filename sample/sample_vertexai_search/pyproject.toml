[project]
name = "sample-vertexai-search"
version = "0.1.0"
description = "Sample code for Vertex AI Search"
dependencies = [
    "google-cloud-discoveryengine>=1.11.0",
    "python-dotenv>=1.0.0",
]
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"